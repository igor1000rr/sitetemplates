<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- Приложение использует jsonb → тесты гоняются на Postgres.
             DB_DATABASE форсим на "testing", чтобы RefreshDatabase не затёр dev-БД.
             Хост/пользователь/пароль берутся из окружения (.env или CI). -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_DATABASE" value="testing" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <!-- В тестах вебхук вызывается с локального IP — явным флагом разрешаем
             обойти IP-allowlist ЮKassa (в prod/staging флаг выключен). -->
        <env name="YUKASSA_ALLOW_INSECURE_WEBHOOK" value="true"/>
    </php>
</phpunit>
